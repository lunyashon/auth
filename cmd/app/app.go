package main

func main() {
	// internal.Assembling()
	// cfg := config.MustLoad()
	// log := loger.WriteLog(cfg.Env)
	// application := internal.New(log, cfg.GRPS.Port, cfg.TokenJWT)
	// if err := application.GRPCServer.Run(); err != nil {
	// 	fmt.Println(err)
	// }
}
